# Python Signup Form with SQLite

Built-in **http.server** serves `signup.html`; **POST /signup** stores user data in **SQLite** (`users.db`) and shows a success page.

**Stack:** Python · http.server · SQLite · HTML

**Run:** `python server.py` → open http://localhost:8000
